version: '3.8'

services:
  minebot:
    image: wbxl0/minebot:latest
    container_name: minebot-assistant
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      # OpenAI API configuration (optional, can also be set via web UI)
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - OPENAI_BASE_URL=${OPENAI_BASE_URL:-}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-3.5-turbo}
      - BYPASS_SERVICE_URL=${BYPASS_SERVICE_URL:-}
    volumes:
      # Persist configuration data
      - minebot-data:/app/server/data
    networks:
      - minebot-network
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/api/auth/check" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  bypass-service:
    profiles:
      - bypass
    build:
      context: ./bypass-service
      dockerfile: Dockerfile
    container_name: minebot-bypass
    restart: unless-stopped
    ports:
      - "5000:5000"
    networks:
      - minebot-network
    shm_size: 2gb
    environment:
      - PYTHONUNBUFFERED=1
      - DISPLAY=:99
      - SE_OFFLINE=1
    volumes:
      - ./data:/app/data
      - ./output:/app/output
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1024M
        reservations:
          cpus: '0.2'
          memory: 256M
volumes:
  minebot-data:
    driver: local

networks:
  minebot-network:
    driver: bridge
